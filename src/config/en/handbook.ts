/**
 * Handbook configuration (English)
 * Content lives in src/content/handbook/en/*.md
 */

import { loadHandbookEntries } from '../../content/handbook/load';

export const handbook = loadHandbookEntries('en');
